# Demo
Demonstrates the use of the 'capacitor-blob-writer' plugin.

## Instructions

```
cd demo && \
  npm i && \
  npx cap update && \
  npx cap copy &&
  npm run watch &

npx cap open android
npx cap open ios
```
